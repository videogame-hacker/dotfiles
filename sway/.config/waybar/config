{
    "layer": "bottom",
    "modules-left": [ "sway/workspaces", "sway/mode", "mpd" ],
    "modules-center": [ "sway/window" ],
    "modules-right": [ "pulseaudio", "clock", "tray" ],
    "height": 30,

    "mpd": {
        "format": "{artist} - {title}",
        "format-stopped": "",
        "format-disconnected": ""
    },

    "clock": {
        "format": "{: %a %-d %b, %H:%M}"
    },

    "pulseaudio": {
        "format": "Volume: {volume}%",
        "format-muted": "Volume: 0%",
        "on-click": "sh -c 'pactl set-sink-mute $(pactl info | grep \"Default Sink\" | cut -d \" \" -f3) toggle'"
    }
}
